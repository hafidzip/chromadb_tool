[project]
name = "chromadb_tool"
version = "0.1.0"
description = "Store and query vector embeddings using ChromaDB."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chromadb>=0.4.0",
    "openchadpy>=0.1.23",
]
